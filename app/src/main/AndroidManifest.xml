<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.proiectandroidretrofit">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".SarbatoriLegaleActivity"></activity>
        <activity android:name=".EditUser" />
        <activity android:name=".DetaliiDepozitBD" />
        <activity android:name=".ListaBDUsers" />
        <activity android:name=".ListaBDDepozite" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ConvertorDepozite" />
        <activity android:name=".ConvertorCredite" />
        <activity android:name=".ListaCrediteActivity" />
        <activity android:name=".ListaDepoziteActivity" />
        <activity android:name=".DepozitActivity" />
        <activity android:name=".CreditActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>